.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
# Local dev storage root — real uploaded bytes, never commit.
.storage-dev/
# Keys minted during a test-script run (path keys of the run — noise, not secret).
test-scripts/fixtures/local-keys.env
